/*
To Uppercase
Create a String by surrounding characters with an apostrophe or quotation marks. Call the toUpperCase() method on the string.

EX: "apples".toUpperCase() OUTPUT: "APPLES"
*/

// YOUR CODE BELOW


//set a variable to a string with all lowercase characters

const redFruit = "apples";

//use string method, toUpperCase() to convert lowercase characters to uppercase

//The toUpperCase() method returns the calling string value converted to uppercase (the value will be converted to a string if it isn't one).

console.log(redFruit.toUpperCase());//--> "APPLES"